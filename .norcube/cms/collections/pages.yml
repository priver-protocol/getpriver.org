label: Pages
description: All site pages, including homepage and deep-dive articles.
hasBody: false
fields:
  - key: title
    type: enum
    label: Title
    options:
      - Priver — Your message. Nobody else's.
      - Priver for Journalists — Protect your sources. Protect yourself.
      - The threat model — Priver
  - key: seo
    type: object
    label: SEO
    fields:
      - key: title
        type: enum
        label: SEO Title
        options:
          - Priver — Your message. Nobody else's.
          - Priver for Journalists — Protect your sources. Protect yourself.
          - The threat model — Priver
      - key: description
        type: enum
        label: SEO Description
        options:
          - A messenger with no one in the middle. No accounts. No phone numbers. No servers. No company that can read, scan, or han…
          - Your source trusts you with their safety. Priver is the only messenger where there's no server to subpoena, no metadata …
          - How every mainstream messenger leaks your data, why the EU's Chat Control breaks them all, and what Priver's architectur…
      - key: canonical
        type: url
        label: Canonical URL
      - key: og_image
        type: image
        label: Open Graph Image
        accept: image/*
        maxSize: 5242880
      - key: og_type
        type: enum
        label: Open Graph Type
        options:
          - website
          - article
      - key: twitter_card
        type: enum
        label: Twitter Card
        options:
          - summary
    preview:
      title: [{key: title}]
      description: [{key: description}]
      image: [{key: og_image}]
  - key: blocks
    type: blocks
    label: Page Blocks
    components:
      - hero
      - feature-grid
      - cta-banner
      - protocol-section
      - download-cta
      - split-feature
      - scenario-cards
      - article-cards-grid
      - chat-control-callout
      - comparison-table
