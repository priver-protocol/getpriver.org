label: Split Feature
description: A feature section with heading, description, bullets, and callout.
fields:
  - key: id
    type: enum
    label: ID
    options:
      - problem
      - how-it-works
  - key: label
    type: enum
    label: Label
    options:
      - The problem
      - How it works
  - key: heading
    type: enum
    label: Heading
    options:
      - The tools you trust *are already compromised.*
      - Built for the people who *need privacy most.*
  - key: description
    type: enum
    label: Description
    options:
      - You use Signal because it's "the gold standard." But Signal still requires a phone number — a number tied to your iden…
      - Priver isn't a company making promises. It's a protocol making promises impossible to break.
  - key: bullets
    type: array
    label: Bullets
    itemType:
      type: object
      label: Bullet
      fields:
        - key: icon
          type: enum
          label: Icon
          options:
            - lucide:phone
            - lucide:server
            - lucide:cloud
            - lucide:key-round
            - lucide:route
            - lucide:lock
            - lucide:network
        - key: text
          type: enum
          label: Text
          options:
            - '**Signal** requires your phone number. That number links to your carrier, your billing address, and your call history.'
            - '**WhatsApp** hands over metadata in near real time — who you talk to, when, and how often. A leaked FBI document confi…'
            - '**Telegram** is not end-to-end encrypted by default. After the CEO''s arrest in France, it now hands IP addresses and pho…'
            - '**No phone number, no account.** Your identity is a key pair generated on your device. Share it via QR code or link. Nob…'
            - '**Messages travel through Tor.** No IP address exposed. No location metadata. No way to prove two devices communicated.'
            - '**Signal Protocol encryption — without Signal''s servers.** The same Double Ratchet algorithm, with forward secrecy. Ea…'
            - '**Decentralized discovery.** Find other Priver users through a distributed hash table — no central directory that can …'
  - key: callout
    type: object
    label: Callout
    fields:
      - key: quote
        type: enum
        label: Quote
        options:
          - A journalist's right to protect their sources means nothing if the messenger itself is the evidence.
          - 'Signal proved that end-to-end encryption could be easy. Priver takes the next step: removing the servers, the phone numb…'
  - key: background
    type: enum
    label: Background
    options:
      - white
  - key: reverse
    type: boolean
    label: Reverse
preview:
  title:
    - key: heading
    - key: label
  description:
    - key: description
  image: []
