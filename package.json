{
  "name": "getpriver-org",
  "version": "1.0.0",
  "description": "Landing page for the Priver protocol — a serverless, end-to-end encrypted messaging protocol.",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ./output.css --minify",
    "dev": "tailwindcss -i ./input.css -o ./output.css --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/priver-protocol/getpriver.org.git"
  },
  "keywords": [
    "priver",
    "privacy",
    "messaging",
    "end-to-end-encryption",
    "tor",
    "decentralized",
    "open-protocol"
  ],
  "author": "Jetlio",
  "license": "MIT",
  "homepage": "https://getpriver.org/",
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
